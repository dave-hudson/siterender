import resolve from '@rollup/plugin-node-resolve';
import commonjs from '@rollup/plugin-commonjs';
import typescript from '@rollup/plugin-typescript';
import json from '@rollup/plugin-json';

export default {
  input: 'src/siterender.ts',
  output: {
    file: 'build/siterender.mjs',
    format: 'esm',
    sourcemap: true,
    inlineDynamicImports: true // Ensure all dynamic imports are inlined
  },
  plugins: [
    resolve({
      preferBuiltins: true, // Prefer built-in modules (e.g., fs, path)
      browser: false, // Keep this false to correctly bundle node modules
    }),
    commonjs({
      include: 'node_modules/**', // Include dependencies from node_modules
      transformMixedEsModules: true, // Handle mixed ES and CommonJS modules
    }),
    typescript({
      tsconfig: './tsconfig.json',
    }),
    json() // Enable JSON imports
  ],
  external: [
    'fs',
    'path',
    'os',
    'http',
    'https',
    'axios',
    'puppeteer',
    'yargs',
    'fast-xml-parser',
  ],
};

